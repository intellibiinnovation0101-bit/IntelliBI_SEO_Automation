Place the IntelliBI service account key here as:  service_account.json
(the SAME key used by IntelliBI_Operations_Automation).

Then share, with the key's client_email
(intellibi-data-pipeline@intellibi-mis.iam.gserviceaccount.com):
  - the source Google Sheet  -> Viewer
  - the Drive output folder  -> Editor
